# ⚡ 环境变量配置文件（敏感信息）
# 
# 使用说明：
# 1. 复制此文件为 `.env`
# 2. 填写你的敏感信息
# 3. .env 文件已在 .gitignore 中，不会被提交到 Git
# 
# 配置优先级：环境变量 > .env > config.toml > 代码默认值
#
# 💡 提示：
#   - 敏感信息（密码、API keys）→ .env（本文件）
#   - 应用配置（阈值、路径）→ config.toml
#   - GitHub Actions 用户 → 只需设置 GitHub Secrets，无需此文件
#
# 📌 环境变量格式说明：
#   使用双下划线 __ 访问嵌套字段（推荐）：API__SYSID, NOTIFICATION__SMTP_HOST

# =============================================================================
# ECUST API 配置（必填）
# =============================================================================
# 如何获取这些参数：
# 1. 浏览器访问 ECUST 电费查询页面
# 2. 按 F12 打开开发者工具 → Network 标签
# 3. 查询电量，找到请求 URL 中的参数

API__SYSID=your_sysid_here
API__ROOMID=your_roomid_here
API__AREAID=your_areaid_here
API__BUILDID=your_buildid_here


# =============================================================================
# 通知配置（可选 - 敏感信息）
# =============================================================================

# --- 邮件通知密码 ---
# Gmail: 使用应用专用密码 https://myaccount.google.com/apppasswords
# QQ: 使用授权码 https://service.mail.qq.com/cgi-bin/help?subtype=1&&id=28&&no=1001256
NOTIFICATION__SMTP_PASSWORD=your_app_password_here

# 发件人邮箱（可选，也可在 config.toml 中配置）
# NOTIFICATION__SMTP_USER=your_email@gmail.com

# 收件人邮箱（可选，也可在 config.toml 中配置）
# NOTIFICATION__RECIPIENTS=["recipient@example.com"]


# --- Server酱 SendKey ---
# 获取 SendKey：访问 https://sct.ftqq.com/ 注册并获取
NOTIFICATION__SERVERCHAN_SENDKEY=your_sendkey_here


# =============================================================================
# 可选：覆盖 config.toml 中的配置
# =============================================================================
# 取消注释以下行可覆盖 config.toml 中的默认值

# APP__ALERT_THRESHOLD_KWH=10.0
# APP__CHECK_INTERVAL_SECONDS=3600
# APP__LOG_LEVEL=INFO

# API__TIMEOUT_SECONDS=10
# API__MAX_RETRIES=3

# STORAGE__DATA_DIR=data
# STORAGE__CSV_FILENAME=electricity.csv

# NOTIFICATION__CHANNELS=["email", "serverchan"]
# NOTIFICATION__SMTP_HOST=smtp.gmail.com
# NOTIFICATION__SMTP_PORT=587
# NOTIFICATION__SMTP_STARTTLS=true

# REPORT__DAYS_TO_ANALYZE=7
# REPORT__OUTPUT_DIR=output/reports


